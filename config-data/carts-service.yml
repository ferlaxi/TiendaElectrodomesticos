server:
  port: 8083

spring:
  application:
    name: carts-service

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect

  datasource:
    url: jdbc:mysql://localhost:3306/carts-service?serverTimezone=UTC
    username: root
    password: ""

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
